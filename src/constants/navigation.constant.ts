export enum MainStack {
  Private = 'Private',
  Public = 'Public',
}

export enum AuthStack {
  Dashboard = 'Dashboard',
  Home = 'Home',
  Profile = 'Profile',
}

export enum NonAuthStack {
  EnterPhone = 'EnterPhone',
  EnterOTP = 'EnterOTP',
  Phone = 'Phone',
}
